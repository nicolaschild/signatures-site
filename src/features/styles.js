import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  container: {
    height: '100vh',
    width: '100vw',
  },
  boxLeft: {
    height: '100%',
    background: '#000',
  },
  boxRight: {
    height: '100%',
    background: '#fff',
  },
});
